@model IEnumerable<string>

@Html.ActionLink("Home", "Menu", "Nav");


@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Product",
    action = "List",
    category = link,
    page = 1
})
}
